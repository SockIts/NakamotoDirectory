# NSIDirectory
Scaffold project created from the current Bitoshi-style layout.

## Current status
- UI/layout cloned and rebranded to **NSIDirectory**
- Library + pagination + trait filters + dark mode are wired
- Placeholder trait manifest added so the app runs immediately

## Next session TODO
1. Replace placeholder manifest with real NSIDirectory trait layers
2. Import NSIDirectory trait assets
3. Tune rarity buckets/weights
4. Update labels/branding text as needed